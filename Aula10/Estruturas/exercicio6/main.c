#include <stdio.h>
#include <string.h>

#define COL 3
#define LIN 30
typedef struct {

	char cafeManha[COL][LIN];
	char Almoco[COL][LIN];
	char cafeTarde[COL][LIN];
	char janta[COL][LIN];

} Cardapio;

void main(){

    Cardapio c[7];
    
}